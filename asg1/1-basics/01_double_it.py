# Ask the user to enter a number
curr_value = int(input("Enter a number: "))

# Double the value and print it until it's 100 or more
while curr_value < 100:
    # Double the current value
    curr_value = curr_value * 2

    # Print the new value
    print(curr_value)
